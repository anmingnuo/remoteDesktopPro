server:
  port: 8081

spring:
  application:
    name: remoteServer

  datasource:
    # 配置连接数据库信息
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/remote?characterEncoding=utf-8&useSSL=false
    username: root
    password: whanamn!
mybatis-plus:
  type-aliases-package: com.remotedesktop.entities
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

